<div class="detail-page">
    <div class="articles-container">
        <div class="header">
            <h1>
                Articles
            </h1>
            <%= link_to "Create", new_article_path, class: "btn btn-success create-button" if logged_in? %>

        </div>

        <%= render "partials/articles" %>

    </div>
</div>