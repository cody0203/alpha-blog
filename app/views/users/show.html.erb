<div class="user-detail-page-container container">
    <div class="articles-container">
        <div class="header">
            <h1 class="my-4">
                <%= @user.username %>'s profile
            </h1>

            <%= gravatar_for @user, size: 200 %>

            <%= link_to "Edit yours profile", edit_user_path(@user), class: "btn btn-info my-3"  %>

        </div>

        <h2 class="text-center">
            Articles
        </h2>


        <%= render "partials/articles", @aritlces %>
    </div>
</div>